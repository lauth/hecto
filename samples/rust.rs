pub struct FileType {
    name: String,
}

impl FileType {
    pub fn name(&self) -> String {
        self.name.clone()
    }

    // Single line comment.
	   pub fn test() -> void {
								println!("Ceci est une string");
        let char c = 'x';
								let char c2 = '\t'; // tabulation.
				}
}

